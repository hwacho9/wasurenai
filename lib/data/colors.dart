import 'package:flutter/material.dart';

class AppColors {
  static const primarySwatch = Colors.blue;
  static const scaffoldBackground = Color.fromARGB(255, 227, 224, 221);
  static const appBarBackground = Color.fromARGB(255, 209, 95, 104);
  static const cardBackground = Colors.white;
  static const cardShadow =
      Color.fromRGBO(0, 0, 0, 0.2); // Adjust opacity as needed
  static const textPrimary = Colors.black;
  static const textSecondary = Colors.grey;
  static const iconColor = Colors.grey;
}
